# Example environment variables for backend (backend/.env.example)
# Replace values with your production secrets and hosts

# MongoDB connection string (use Mongo Atlas or other Mongo host)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# Port (Optional: default 3001)
PORT=3001

# FRONTEND_URL - comma-separated allowed origins for CORS (set to your Vercel URL)
FRONTEND_URL=https://your-appchat-frontend.vercel.app

# Session and JWT secrets
SESSION_SECRET=super-secret-session-key
JWT_SECRET=super-secret-jwt-key

# Node environment
NODE_ENV=production

# Google OAuth (optional; only if using Google login)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# When deployed, callback must match this exact URL in Google console
GOOGLE_CALLBACK_URL=https://your-backend-host.com/api/auth/google/callback
